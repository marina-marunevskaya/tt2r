import React from 'react';

import { useTitle } from '../../hooks';

export const Employees = () => {
    useTitle('Сотрудники');

    return (
        <div>
            Сотрудники
        </div>
    );
};
